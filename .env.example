# Copie ce fichier en .env.local et remplis tes propres cles (jamais commit sur GitHub)

# --- Supabase (ton projet > Project Settings > API) ---
# Utilisees des maintenant, pour la phase precommande (formulaire /api/precommande).
NEXT_PUBLIC_SUPABASE_URL=https://xxxx.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJ...
SUPABASE_SERVICE_ROLE_KEY=eyJ...

# --- Stripe (dashboard.stripe.com > Developpeurs > Cles API) ---
# Pas necessaires pour la phase precommande actuelle : aucun paiement n'est pris.
# A remplir plus tard, au vrai lancement 2027, quand app/api/checkout repassera en service.
STRIPE_SECRET_KEY=sk_test_...
STRIPE_WEBHOOK_SECRET=whsec_...
